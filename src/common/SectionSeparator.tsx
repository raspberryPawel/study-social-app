import React from "react";
import styled from "styled-components";

const Separator = styled.div`
	border-bottom: 0.5px solid #878b91;
	opacity: 0.5;
	margin: 5px 0;
`;

export const SectionSeparator = () => (
	<Separator />
);
